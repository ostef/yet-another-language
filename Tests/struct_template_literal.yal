#include "basic.yal.ignore";

const Vec2 = struct($T: type_id) {
    var x: T;
    var y: T;
}

const main = func() -> s32 {
    var a = Vec2(float).{1, 2};
    Assert(a.x == 1);
    Assert(a.y == 2);

    // Inferred
    var b: Vec2(float) = .{1, 2};
    Assert(b.x == 1);
    Assert(b.y == 2);
}
