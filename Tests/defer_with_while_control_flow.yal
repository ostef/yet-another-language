#include "basic.yal.ignore";

const main = func() -> s32 {
    var i = 0;

    defer i += 1;

    while i < 10 {
        defer i += 1;

        if i == 4 {
            break;
        }
    }

    WriteInt(i);
    WriteString("\n");

    Assert(i == 5);
}
