//import "Math";

var a := 10;
var a := "redeclared";
var b := "Hello World";

proc Main (args : []string)
{
    proc ProcInsideProc (a : int, b : string) -> (float, ok : bool)
    {
        var c := 10.0;
        undeclared;

        return a + b * c, false;
    }
}
